# Core agent: no dependencies (stdlib only)
# ==========================================
# These are only needed for specific features:
pyinstaller>=6.0          # Build standalone EXEs via builder.py
pillow>=10.0.0            # Screenshot display in C2 GUI
mss>=9.0.0                # Screenshot capture (alternative)
opencv-python-headless>=4.8.0  # Webcam capture
cryptography>=41.0.0      # Browser password decryption
